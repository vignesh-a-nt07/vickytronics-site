apiVersion: v1
kind: Service
metadata:
  name: {{ include "lms.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "lms.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
